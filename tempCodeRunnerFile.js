let characters = await camp.characters.get();
        // for (let i = 0; i < Object.keys(characters["data"]); i++) {
        //     if (characters["data"][i]["name"] == "Lazarus Flameclaw") {
        //         console.log(characters["data"][i])
        //     } else {

        //     }
        // }